#include <iostream>
using namespace std;

int main(){
    cout<< "Hii welcome";
/*    int num,i;
    int array[30];

    cout<<"Enter elements:";
    cin>>num;

    for ( i = 0; i < num; i++)
    {
        cout<< "Numbers: " << i <<" :";
        cin>>arr[i];
    }
    int max=array[0]
    for ( i = 0; i < num; i++)
    {
        if (max<array[i])
        {
            max=array[i];
        }
    }
    cout<< endl << "Largest element :" << max;
 */   
}